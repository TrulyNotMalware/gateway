spring:
  config:
    activate:
      on-profile: "local"
  lifecycle:
    timeout-per-shutdown-phase: 10s
  cloud:
    gateway:
      server:
        webflux:
          # Local: trust all IPs (regex matches everything).
          trusted-proxies: ".*"
          httpclient:
            connect-timeout: 3000
            response-timeout: 10000
          globalcors:
            cors-configurations:
              '[/**]':
                allowed-origin-patterns:
                  - "http://localhost:*"
                  - "http://127.0.0.1:*"
                allow-credentials: true
                allowed-methods:
                  - PUT
                  - GET
                  - POST
                  - DELETE
                  - OPTIONS
                  - PATCH
                allowed-headers:
                  - "*"
                exposed-headers:
                  - X-Request-ID
                  - X-RateLimit-Remaining
                  - X-RateLimit-Reset
                  - Retry-After

server:
  port: 8080
  shutdown: graceful
  forward-headers-strategy: none

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,gateway,metrics,env,configprops
      base-path: /actuator
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true
    circuitbreakers:
      enabled: true
  metrics:
    tags:
      application: gateway
  prometheus:
    metrics:
      export:
        enabled: true

app:
  config:
    jwt:
      issuers:
        - issuer: "blog-be"
          jwks-uri: "http://localhost:8000/.well-known/jwks.json"
          audience: "blog-be"
    security:
      enable-blacklist: false
      enable-rate-limit: false
      timeout-ms: 1000
